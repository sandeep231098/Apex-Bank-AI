import Typography from "@mui/material/Typography";

export default function Logo() {
  return (
    <Typography
      variant="h4"
      sx={{
        fontWeight: 800,
        color: "primary.main",
        letterSpacing: 1,
      }}
    >
      Apex Bank AI
    </Typography>
  );
}